function solution(n, lost, reserve) {
  var answer = 0;

  // 0. 전체 체육복수는 현재 5명ㅇ임 
  // . 중복의 제거, lost에있는 번호와 reseve에 있는 번호가 같으면 reserve에있는 번호는 빼준다. (filter)
  // 1. 잃어버린 학생의 번호의 앞뒤번호가 reserve에 있으면 count++ 해준다. 
  // 2. 만약 그 앞뒤번호가 reserve에 두개있으면 
  return answer;
}
console.log(solution(	5, [2, 4], [1, 3, 5]); // 5
console.log(solution(	5, [2, 4 , 3], [1, 3, 5]); // 5


